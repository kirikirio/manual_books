= render 'shared/sub-header'
.seisou-header
  マニュアル一覧
  %span（清掃）
  .seisou-setumei
    読みたいマニュアルをクリック👈
.seisou__manual
  - @seisou.each do |manual|
    .seisou__manual__box
      = link_to manual_path(manual.id) do
        = manual.name
= render 'shared/footer'