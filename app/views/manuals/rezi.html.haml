= render 'shared/sub-header'
.rezi-header
  マニュアル一覧
  %span（接客・販売）
  .rezi-setumei
    読みたいマニュアルをクリック👈
.rezi__manual
  - @rezi.each do |manual|
    .rezi__manual__box
      = link_to manual_path(manual.id) do
        = manual.name
= render 'shared/footer'