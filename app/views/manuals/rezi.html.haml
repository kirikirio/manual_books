= render 'shared/sub-header'
.rezi-header
  マニュアル一覧
  %span（レジ）
  .rezi-setumei
    読みたいマニュアルをクリック👈
.rezi__manuals
  - @rezi.each do |manual|
    .rezi__manuals__box
      = link_to manuals_path(manual.id) do
        = manual.name