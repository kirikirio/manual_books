= render 'shared/sub-header'
.cofee-header
  マニュアル一覧
  %span（コーヒーマシン）
  .cofee-setumei
    読みたいマニュアルをクリック👈
    .flyer__manuals
      - @flyer.each do |manual|
        .flyer__manuals__box
          = link_to manuals_path(manual.id) do
            = manual.name