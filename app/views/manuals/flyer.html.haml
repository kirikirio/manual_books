= render 'shared/sub-header'
.cofee-header
  マニュアル一覧
  %span（清掃）
  .cofee-setumei
    読みたいマニュアルをクリック👈
    .flyer__manual
      - @flyer.each do |manual|
        .flyer__manual__box
          = link_to manual_path(manual.id) do
            = manual.name
= render 'shared/footer'