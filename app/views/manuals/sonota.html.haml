= render 'shared/sub-header'
.sonota-header
  マニュアル一覧
  %span（その他）
  .sonota-setumei
    読みたいマニュアルをクリック👈
.sonota__manual
  - @sonota.each do |manual|
    .sonota__manuals__box
      = link_to manual_path(manual.id) do
        = manual.name
= render 'shared/footer'