= render 'shared/sub-header'
.sekkyaku-header
  マニュアル一覧
  %span（接客）
  .sekkyaku-setumei
    読みたいマニュアルをクリック👈
.sekkyaku__manual
  - @sekkyaku.each do |manual|
    .sekkyaku__manual__box
      = link_to manual_path(manual.id) do
        = manual.name
= render 'shared/footer'