= render 'shared/sub-header'
.cofee-header
  マニュアル一覧
  %span（発注）
  .cofee-setumei
    読みたいマニュアルをクリック👈
.cofee__manual
  - @cofee.each do |manual|
    .cofee__manual__box
      = link_to manual_path(manual.id) do
        = manual.name
= render 'shared/footer'