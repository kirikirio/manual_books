= render 'shared/sub-header'
.cofee-header
  マニュアル一覧
  %span（発注）
  .cofee-setumei
    読みたいマニュアルをクリック👈
.cofee__manuals
  - @cofee.each do |manual|
    .cofee__manuals__box
      = link_to manual_path(manual.id) do
        = manual.name
= render 'shared/footer'